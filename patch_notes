commit 11

created PointsThisWeek sql function. Params: week, nflteam abbr, instancenumber. Returns: a decimal of instancenumber if this team won, half if tied, 0 if lost or bye. works
created PointsSeason. works
created TeamPointsThisWeek. works.
created EndOfWeek. works
created initial_setup.py which wipes and then creates everything in the database. works. 


//call updatedb.py either scheduled or on refresh. currently, I run update.py from command line

